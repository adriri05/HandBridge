// HandBridge mobile app — skeleton
// TODO: BLE scan/connect via react-native-ble-plx, decode gestures, display + TTS.

import React from "react";
import { SafeAreaView, Text } from "react-native";

export default function App() {
  return (
    <SafeAreaView>
      <Text>HandBridge</Text>
      <Text>Live gesture output will appear here.</Text>
    </SafeAreaView>
  );
}
